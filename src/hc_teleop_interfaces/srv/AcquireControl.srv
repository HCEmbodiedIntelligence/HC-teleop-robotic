# Priority is deliberately absent. The arbiter resolves source priority from
# trusted configuration rather than accepting it from a client.
string source_id
string session_id
string[] resources
builtin_interfaces/Duration requested_duration
---
bool granted
string lease_id
builtin_interfaces/Time expires_at
string reason
